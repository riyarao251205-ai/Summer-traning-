class BillSystem:

    @staticmethod
    def calculate_bill():
        devices = 8
        rate_per_unit = 8 
        total_units = 0
        for i in range(1, devices + 1):
            device_name = input("enter device name ")
            watts = float(input("enter watts "))
            hours = float(input("enter hours "))
            days = int(input("enter no of days "))
            units = (watts * hours * days) / 1000      
            total_units += units
            avg= total_units/devices

           print(units)
           print(total_units)
        total_bill = total_units* rate_per_unit
        print(total_bill)
        print(rate_per_unit)
BillSystem.calculate_bill()
